import styled from "@emotion/styled";
import "../index.css"

export const Container=styled.div`
    margin: auto;
    width:max-content;
    min-height: 10rem;
    padding: 2rem;
    box-shadow: 0 1rem 4rem -2rem var(--hgColor);

`